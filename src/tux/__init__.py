"""Tux: a terminal-native Linux assistant that proposes commands in plain English.

The version below is the single source of truth; packaging metadata reads
it dynamically (see ``[tool.setuptools.dynamic]`` in ``pyproject.toml``).
"""

__version__ = "0.2.0"
